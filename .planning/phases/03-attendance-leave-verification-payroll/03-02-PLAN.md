---
phase: 03-attendance-leave-verification-payroll
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/payroll/payroll-management.cron.ts
  - src/payroll/update-payroll.controller.ts
  - openapi/paths/payroll/approve-id.yaml
  - openapi/openapi.yaml
autonomous: true
requirements:
  - PAYROLL-02
user_setup: []
must_haves:
  truths:
    - "Payroll generation explicitly verifies Leave status against 'approved' records."
    - "Payroll updates (payments) rejected unless status is 'approved'"
  artifacts:
    - path: "src/payroll/payroll-management.cron.ts"
      provides: "Attendance/Leave logic enforcement"
    - path: "src/payroll/update-payroll.controller.ts"
      provides: "Payment logic constraint"
    - path: "openapi/paths/payroll/approve-id.yaml"
  key_links:
    - from: "src/payroll/payroll-management.cron.ts"
      to: "src/database/leave.model.ts"
      via: "Approved leaves check"
    - from: "src/payroll/update-payroll.controller.ts"
      to: "src/database/payroll.model.ts"
      via: "Status validation"
    - from: "openapi/openapi.yaml"
      to: "openapi/paths/payroll/approve-id.yaml"
      via: "Path registration"
---

<objective>
Integrate attendance/leave validation into the payroll generation cron and restrict payment updates to approved records. Finalize phase documentation.

Purpose: Ensure payroll estimates are accurate and payments are authorized.
Output: Updated cron logic, secured payment endpoint, updated OpenAPI docs.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-attendance-leave-verification-payroll/03-RESEARCH.md
@src/payroll/payroll-management.cron.ts
@src/payroll/update-payroll.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Attendance/Leave Validation in Cron</name>
  <read_first>
    @src/payroll/payroll-management.cron.ts
    @src/database/leave.model.ts
  </read_first>
  <files>src/payroll/payroll-management.cron.ts</files>
  <action>
    In `src/payroll/payroll-management.cron.ts`, update the logic to fetch approved leaves (`Leave` model, status 'approved') for the calculation period. Map attendance status ('on-leave', 'half-day') based on approved leave records, not just attendance table flags.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Cron uses approved leaves for payroll estimation</done>
</task>

<task type="auto">
  <name>Task 2: Restrict Payment Updates and Finalize Docs</name>
  <read_first>
    @src/payroll/update-payroll.controller.ts
    @openapi/openapi.yaml
    @openapi/paths/payroll/root.yaml
  </read_first>
  <files>src/payroll/update-payroll.controller.ts, openapi/paths/payroll/approve-id.yaml, openapi/openapi.yaml</files>
  <action>
    In `src/payroll/update-payroll.controller.ts`, add a guard to ensure `Payroll` status is 'approved' before allowing payment ('paid'/'partially-paid') updates. Verify `openapi/paths/payroll/approve-id.yaml` exists (created in plan 01) and is registered in `openapi/openapi.yaml`; fix if missing. Run `pnpm docs:lint`, then `graphify update .` to finalize phase closeout.
  </action>
  <verify>
    <automated>pnpm docs:lint && pnpm lint</automated>
  </verify>
  <done>Payment restricted to approved payroll, OpenAPI updated, graph synced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron→Logic | Payroll generation fetches from Attendance/Leave models. |
| Admin→Payment | Payment controller enforces status before update. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege | Cron | mitigate | Cross-reference with approved Leaves collection. |
| T-03-04 | Tampering | Payment | mitigate | Enforce status check in `update-payroll.controller.ts`. |
</threat_model>

<verification>
Ensure payroll generation correctly applies leave logic and payment endpoints reject unapproved records.
</verification>

<success_criteria>
- Payroll calculation accurately reflects approved leaves.
- Payment updates guarded by status check.
- Documentation up to date.
</success_criteria>
