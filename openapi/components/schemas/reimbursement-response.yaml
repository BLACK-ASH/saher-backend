allOf:
  - $ref: './envelope.yaml'
  - type: object
    properties:
      data:
        type: object
        properties:
          id:
            type: string
          user:
            type: string
          amount:
            type: number
          description:
            type: string
          status:
            type: string
            enum: [pending, accept, reject, on-hold]
