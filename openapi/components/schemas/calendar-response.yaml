allOf:
  - $ref: './envelope.yaml'
  - type: object
    properties:
      data:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
            title:
              type: string
            date:
              type: string
              format: date-time
            type:
              type: string
      meta:
        $ref: './meta-pagination.yaml'
