allOf:
  - $ref: './envelope.yaml'
  - type: object
    properties:
      data:
        type: object
        properties:
          id:
            type: string
          title:
            type: string
          content:
            type: string
          expiresAt:
            type: string
            format: date-time
