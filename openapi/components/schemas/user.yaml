type: object
properties:
  id:
    type: string
  name:
    type: string
  email:
    type: string
    format: email
  role:
    type: string
    enum: [admin, manager, user, intern]
  displayName:
    type: string
