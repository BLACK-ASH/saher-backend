get:
  tags: [Reimbursement]
  summary: Get soft-deleted reimbursement bills
  operationId: getRecycleReimbursementBills
  responses:
    '200':
      description: Recycled bills list
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/reimbursement-response.yaml
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: ../../components/common/error-response.yaml
