post:
  tags: [Attendance]
  summary: Cron - Auto Checkout Open Records At Shift End
  operationId: cronAutoCheckout
  parameters:
    - name: pass
      in: path
      required: true
      schema: { type: string }
  responses:
    '200':
      description: Auto checkouts applied
      content:
        application/json:
          schema:
            $ref: ../../components/common/succes-response.yaml
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: ../../components/common/error-response.yaml
