post:
  tags: [Calendar]
  summary: Sync Google Holidays
  operationId: syncGoogleHolidays
  responses:
    '200':
      description: Holidays synced
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/calendar-response.yaml
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: ../../components/common/error-response.yaml
