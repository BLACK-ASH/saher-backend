get:
  tags: [Leave]
  summary: Get all leave applications (Admin/Manager)
  operationId: getAllLeaveApplications
  responses:
    '200':
      description: All leave applications
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/leave-response.yaml
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: ../../components/common/error-response.yaml
